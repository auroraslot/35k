问题在于你只用了一层循环，但这件事需要两层：外层遍历位置，内层遍历每个字符串。

正确解法：纵向扫描

class Solution {
  public String longestCommonPrefix(String[] strs) {
      // 以第一个字符串为基准，逐个字符检查
      for (int p = 0; p < strs[0].length(); p++) {
          char c = strs[0].charAt(p); // 第一个字符串的第 p 个字符

          // 拿这个字符和其他所有字符串的第 p 个位置比较
          for (int i = 1; i < strs.length; i++) {
              // 越界 或 字符不同 → 结束
              if (p >= strs[i].length() || strs[i].charAt(p) != c) {
                  return strs[0].substring(0, p);
              }
          }
      }

      // 第一个字符串全部匹配完，它本身就是最长公共前缀
      return strs[0];
  }
}